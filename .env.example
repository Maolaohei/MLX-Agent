# MLX-Agent 环境变量配置
# 复制此文件为 .env 并填入真实值
# 
# 安装步骤:
#   1. cp .env.example .env
#   2. 编辑 .env，填入你的 API key 和配置
#   3. 确保 .env 已在 .gitignore 中

# =============================================================================
# LLM API 配置 (必需)
# =============================================================================

# OpenAI API Key
# 获取地址: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# 可选: 自定义 API Base (用于第三方 API 代理)
# OPENAI_API_BASE=https://api.openai.com/v1

# 可选: 额外的认证 Token
AUTH_TOKEN=your_auth_token_here

# =============================================================================
# Telegram Bot 配置 (可选)
# =============================================================================

# Telegram Bot Token
# 获取方式: 在 Telegram 中找 @BotFather，发送 /newbot 创建
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Telegram 管理员用户 ID
# 获取方式: 在 Telegram 中找 @userinfobot，发送任意消息获取
TELEGRAM_ADMIN_ID=your_admin_user_id_here

# =============================================================================
# 插件系统配置 (Phase 2)
# =============================================================================

# 备份恢复插件 - WebDAV 配置 (可选)
# 用于自动备份到 WebDAV 服务器
WEBDAV_URL=https://your-webdav-server.com/dav
WEBDAV_USER=your_webdav_username
WEBDAV_PASS=your_webdav_password

# API 密钥管理插件 - 加密密钥 (必需，如果启用 api-manager 插件)
# 生成方式: openssl rand -base64 32
API_ENC_KEY=your_32_character_encryption_key_here

# =============================================================================
# 记忆系统配置 (可选)
# =============================================================================

# Ollama 配置 (用于本地嵌入模型)
# OLLAMA_HOST=http://localhost:11434

# ChromaDB 服务器配置 (如果使用远程 ChromaDB)
# CHROMA_HOST=localhost
# CHROMA_PORT=8000

# =============================================================================
# 数据库配置 (可选，默认使用 SQLite)
# =============================================================================

# Redis 配置 (如果使用外部 Redis)
# REDIS_URL=redis://localhost:6379/0

# PostgreSQL 配置 (如果使用 PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost/mlx_agent

# =============================================================================
# 高级配置 (可选)
# =============================================================================

# 日志级别: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 调试模式: true 或 false
DEBUG=false

# 健康检查端口
HEALTH_CHECK_PORT=8080

# 工作进程数
MAX_WORKERS=4

# =============================================================================
# 安全提示
# =============================================================================
# 
# 1. 永远不要将包含真实 API key 的 .env 文件提交到 Git!
# 2. 确保 .env 在 .gitignore 中
# 3. 定期轮换 API key
# 4. 使用强密码和加密密钥
# 5. 限制 API key 的权限范围
#
